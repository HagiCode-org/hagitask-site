# HagiTask Site no longer mounts the Community repository as a Git submodule.
# The deploy workflow checks out an exact Community commit at build time into a
# runner-local directory pointed at by HAGITASK_COMMUNITY_SOURCE_DIR, and the
# published Community commit is persisted in community-packages.commit.
